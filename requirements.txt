pillow>=10.0
numpy>=1.24
# Modern Tk theme (native .app UI).
customtkinter>=5.2
# Web UI alternative — pin the Gradio 4.44 stack so sandbox installs stay compatible.
gradio==4.44.1
gradio_client==1.3.0
huggingface_hub>=0.23,<0.25
pydantic>=2.7,<2.11
fastapi>=0.115,<0.116
starlette>=0.40,<0.42
jinja2>=3.1.2,<3.1.5
# Optional HEIC/HEIF input (Gemini iPhone exports, etc.).
pillow-heif>=0.16

# Optional, for the "diffusion regeneration" advanced image mode.
# Uncomment to install; these pull ~2-3 GB of model weights on first run.
# torch>=2.2
# diffusers>=0.30
# transformers>=4.40
# accelerate>=0.30
# safetensors>=0.4
